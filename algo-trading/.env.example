# Fyers API credentials — create an app at https://myapi.fyers.in (Individual App)
# App type: "App for Personal Use" / Individual App, redirect URI must match exactly.
FYERS_CLIENT_ID=XXXXXXXX-100
FYERS_SECRET_KEY=YYYYYYYYYY
FYERS_REDIRECT_URI=https://127.0.0.1:8080/

# Anthropic API key — used by the Claude market analyst (optional but recommended)
ANTHROPIC_API_KEY=sk-ant-...
